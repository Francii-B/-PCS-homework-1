#Exercise 1: Polar coordinates  
import cmath #import the module of complex numbers

compl=complex(input()) #convert the input into a complex number
print abs(compl)    #compute the modulus
print cmath.phase(compl) #compute the main argument
